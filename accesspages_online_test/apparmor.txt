#include <tunables/global>

profile accesspages_online_test flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>

  network inet stream,
  network inet dgram,
  network inet6 stream,
  network inet6 dgram,
  capability setuid,
  capability setgid,
  capability chown,
  capability fowner,
  capability dac_override,
  capability kill,
  signal (receive),
  signal (send) peer=*accesspages_online_test,

  # Python lists the package directory before loading sibling modules.
  /opt/accesspages-test/ r,
  /opt/accesspages-test/** rix,
  /opt/machinectl rix,
  /opt/frpc rix,
  /usr/bin/python3* rix,
  /usr/bin/openssl rix,
  /usr/sbin/nginx rix,
  /sbin/tini rix,
  /bin/** rix,
  /usr/lib/** mr,
  /lib/** mr,
  /etc/** r,
  /run/** rwk,
  /tmp/** rwk,
  /data/ rw,
  /data/** rwk,
  /data/connector/frpc rix,
  /proc/** r,
  /sys/devices/system/cpu/** r,
  /sys/fs/cgroup/** r,
  /dev/null rw,
  /dev/stdout w,
  /dev/stderr w,
  /dev/urandom r,
}
